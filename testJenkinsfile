pipeline{
    agent any

    stages{
        stage('dep install'){
            agent {docker {image 'nodejs'}}
            steps{
                sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }

        }
    }
}